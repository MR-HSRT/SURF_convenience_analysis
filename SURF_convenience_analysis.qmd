---
title: "Convenience_own"
format: html
editor: visual
---

```{r}

# packages
if (!require("pacman")) install.packages("pacman")
pacman::p_load(
  tidyverse,
  here
)

```

```{r eval = F}

# directory setup


```

```{r eval = F}

# load, clean and save data
daten <- readr::read_csv(here("data", "raw", "meine_umfrage.csv"))

```
